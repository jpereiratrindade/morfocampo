fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Transcrição offline (opcional mas recomendado para campo):
# pip install faster-whisper
# O modelo 'small' (~460 MB) é baixado automaticamente na primeira execução.
# faster-whisper
